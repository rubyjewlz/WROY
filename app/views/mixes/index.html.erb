<h1>List of Mixes</h1>
<%= link_to 'New Mix', new_mix_path %>

<table>
	<tr>
		<th>Title</th>
		<th>Description</th>
		<th colspan="2"></th>
	</tr>

	<% @mixes.each do |mix| %>
	<tr>
		<td><%= mix.title %></td>
		<td><%= mix.text %></td>
		<td><%= link_to 'Show', mix_path(mix) %></td>
		<td><%= link_to 'Edit', edit_mix_path(mix) %></td>
	</tr>
	<% end %>
</table>
<br>
<%= link_to 'Home', controller: 'welcome' %>